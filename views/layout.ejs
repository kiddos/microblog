<!DOCTYPE html>

<html>
<head>
  <title><%= title %> - Microblog</title>
  <link rel="stylesheet" type="text/css" href="/bootstrap.min.css">
  <link rel="stylesheet" type="text/css" href="/index.css">
  <% if (title == 'Chat') { %>
    <link rel="stylesheet" type="text/css" href="/chat.css">
  <% } %>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <script type="text/javascript" charset="utf-8" src="/jquery.min.js"></script>
  <script type="text/javascript" charset="utf-8" src="/bootstrap.min.js"></script>
  <script type="text/javascript" charset="utf-8" src="/socket.io.js"></script>
</head>
<body>
  <div class="navbar navbar-inverse navbar-fixed-top">
    <div class="container-fluid">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse"
          data-target="#mainbar">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a href="/" class="navbar-brand">Microblog</a>
      </div>
      <div class="collapse navbar-collapse" id="mainbar">
        <ul class="nav navbar-nav">
          <% if (title == 'Home') { %>
            <li class="active"><a href="/">Home</a></li>
          <% } else { %>
            <li><a href="/">Home</a></li>
          <% } %>

          <% if (!user) { %>
            <% if (title == 'Login') { %>
              <li class="active"><a href="/login">Login</a></li>
            <% } else { %>
              <li><a href="/login">Login</a></li>
            <% } %>

            <% if (title == 'Register') { %>
              <li class="active"><a href="/reg">Register</a></li>
            <% } else { %>
              <li><a href="/reg">Register</a></li>
            <% } %>
          <% } else { %>

            <% if (title == 'Chat') { %>
              <li class="active"><a href="/chat">chat</a></li>
            <% } else { %>
              <li><a href="/chat">chat</a></li>
            <% } %>

            <% if (title == 'Profile') { %>
              <li class="active"><a href="/profile">Profile</a></li>
            <% } else { %>
              <li><a href="/profile">Profile</a></li>
            <% } %>

            <% if (title == user.name) { %>
              <li><img src="<%= user.image %>" alt="user" id="usericon"></li>
              <li class="active"><a href="/u/<%= user.name %>" id="current-user"><%= user.name %></a></li>
            <% } else { %>
              <li><img src="<%= user.image %>" alt="user" id="usericon"></li>
              <li><a href="/u/<%= user.name %>" id="current-user"><%= user.name %></a></li>
            <% } %>

            <li><a href="/logout">Logout</a></li>
          <% } %>
        </ul>
      </div>
    </div>
  </div>

  <%- body %>

  </div>
  <div class="container-fluid">
    <div class="row">
      <div class="col-lg-12 col-sm-12 col-md-12 col-xs-12 footer">
        <footer>
          <p><a target="_blank">By Joseph, StoneLin 2012</a></p>
        </footer>
      </div>
    </div>
  </div>
</body>

<% if (user) { %>
<script type="text/javascript" charset="utf-8" src="/chat.js"></script>
<% } %>
</html>

